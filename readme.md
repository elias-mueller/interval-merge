Merge function for list of intervals.

Time: O(n log(n))
Memory: O(n)

Memory issues could arise since we are creating a second results array.
This could be handled by merging directly into the original array.